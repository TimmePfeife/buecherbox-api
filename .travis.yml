language: node_js
node_js:
  - "node"
services:
  - postgresql
before_script:
  - psql -c 'CREATE DATABASE buecherbox_test_db;' -U postgres
  - psql -c 'CREATE EXTENSION pgcrypto;' -U postgres
script:
  - npm install
  - npm run postinstall
after_script:
  - npm run test
