const bookboxScheme = require('./bookbox.scheme');
const favoritesScheme = require('./favorites.scheme');
const passwordScheme = require('./password.scheme');
const usersScheme = require('./users.scheme');

module.exports = {
  bookbox: bookboxScheme,
  favorites: favoritesScheme,
  password: passwordScheme,
  users: usersScheme
};
